# install pip
sudo apt install python3-pip pipenv

# setup pyenv
curl https://pyenv.run | bash

source ~/.config/zsh/.zshrc

pyenv install 3.11.6
pyenv global 3.11.6

# setup pipenv
pip install virtualenv jupyter-ascending i3ipc pynvim
